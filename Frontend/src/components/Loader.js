import React from "react";
import "../styles/Loader.css"; // Replace with your CSS file

const Loader = () => {
  return (
    <div className="loader">
      <span className="bar"></span>
      <span className="bar"></span>
      <span className="bar"></span>
    </div>
  );
};

export default Loader;
